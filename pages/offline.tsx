import IPageProps from '../interfaces/IPageProps';
import React, { useState } from 'react';

function Offline(props: IPageProps) {
  return (
    <div>
      <h1>Page is offline</h1>
    </div>
  );
}

export default Offline;